# Local launch config for `scripts/launch-all.sh`.
# Each runtime reads its own config block via `CONFIG_FILE`.

proxy:
  port: 3000

convAgent:
  port: 3001
  databaseUrl: postgresql://thoth:thoth@127.0.0.1:5432/thoth
  blobStorage:
    endpoint: https://61caa9d56c5a0143bfda0f07fd01088a.r2.cloudflarestorage.com
    bucket: thoth-obj-store-dev
    region: auto
    folder: conv-agent
  llmDispatchQueue:
    endpoint: http://127.0.0.1:4566
    region: us-east-1
    queueUrl: http://127.0.0.1:4566/000000000000/thoth-llm-completions
    accessKeyId: test
    secretAccessKey: test

kbCurateAgent:
  port: 3002

planningAgent:
  port: 3003
